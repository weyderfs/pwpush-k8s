apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pwpush.fullname" . }}-test-connection"
  labels:
    {{- include "pwpush.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pwpush.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
